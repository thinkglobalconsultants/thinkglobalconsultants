---
const { id, class: className, contain = true } = Astro.props;
---

<section id={id} class={className}>
	<div class={`${contain ? "container flex justify-items-center mx-auto py-16 lg:py-32" : ""}`}>
		<slot />
	</div>
</section>
